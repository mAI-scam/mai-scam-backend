fastapi
uvicorn
pydantic>=2
python-dotenv
requests
# Extras for non-Windows only (fine to include; uv will skip on Windows):
uvloop; platform_system != "Windows"
httptools; platform_system != "Windows"